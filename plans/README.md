# Improvement Plans

Generated by the `improve` skill on 2026-06-13 from repository state `4633b05` on `main`.
Revised after harsh-critic review on 2026-06-13.

These plans are source-code read-only audit outputs. They are written for a follow-up implementation agent and are intentionally self-contained. The revised default execution order is:

1. `000-security-correctness-first.md`
2. `002-dedupe-vitest-discovery.md`
3. `004-run-integration-tests-in-github-ci.md`
4. `001-register-production-sidecar-ipc.md`
5. `003-restore-ui-typecheck-and-coverage.md`
6. `006-expand-ui-coverage-by-route.md`
7. `005-refresh-status-and-readiness-docs.md`

## Why This Order

The harsh-critic pass correctly found the original plan order had gone stale after the security/correctness audit landed. Recon path containment, archive symlink handling, shell-free binary probes, config-poller status handling, artifact payload validation, and PR close idempotence now lead the sequence.

The Vitest discovery issue comes next because the current unit test command runs duplicated workspace tests through `node_modules`, creating noisy and misleading verification. Fixing discovery makes every later gate faster and more trustworthy.

The GitHub CI parity fix follows immediately so hosted CI runs the integration gate already declared by `pnpm run ci`.

The production sidecar IPC registration gap remains important, but sync IPC has an unresolved source-of-truth problem. Execute the non-sync registration pieces only when they can be verified without inventing new sync policy, or first add the explicit sync adapter design requested in Plan 001.

The UI typecheck/coverage and docs plans remain valuable, but they should not outrank known production correctness/security defects.

Plan 003 hit its own STOP condition during execution: adding the full TSX UI tree to coverage dropped overall coverage to about 67.5% with dozens of page/component rows uncovered. The honest first slice is TSX typecheck plus a small, already-tested UI coverage surface. Plan 006 owns the route-by-route UI coverage expansion.

## Evidence Snapshot

- `pnpm typecheck` passed at audit time.
- `pnpm test` passed, but ran 386 files and 6823 tests, including duplicated paths under `node_modules/.pnpm/node_modules/@joyus/...` and `apps/desktop-companion/node_modules/@joyus/...`.
- `pnpm coverage` had no non-100 rows under the current coverage include surface.
- The coverage surface excludes UI source, and root typecheck excludes TSX.
- Harsh-critic verdict: REVISE. The major critique was stale prioritization after SEC-01 through SEC-03 and COR-01 through COR-03 arrived.
- Broad TSX coverage probe on 2026-06-13 passed tests but showed `All files` near 67.5% coverage, with low rows across `src/ui/pages`, `src/ui/hooks`, and many components.

## Considered But Deferred

- Runtime artifact provenance checks in `apps/desktop-companion/src/runtimeExecution.ts` run sequentially. This is a legitimate performance candidate, but it is lower priority than broken production IPC wiring.
- Recon ZIP export buffers the full archive in memory in `apps/desktop-companion/src/sidecar/recon.ts`. The current code appears designed around modest export sizes, so this should be revisited after runtime correctness gates.
- UI IPC helper duplication is real, but consolidation should wait until TSX typecheck and UI coverage are restored.
- Desktop build signing workflow conditions may be stale or incorrectly scoped, and the dogfood build path should get a focused release-hardening plan. This revision records it, but does not implement signing policy without current workflow-run evidence and a clear ad-hoc signing target.
